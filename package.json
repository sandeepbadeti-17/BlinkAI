{
  "name": "blinkai",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "build": "node build.js",
    "watch": "esbuild content.js --bundle --outfile=dist/content.js --platform=browser --target=chrome110 --format=iife --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}